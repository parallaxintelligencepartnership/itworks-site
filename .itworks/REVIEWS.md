# Reviews

## Checkpoint 1 - 2026-09-11 - lenses: security-auth, real-data
Scope: checkpoint, diff 3034ce5..2c914f0 (first checkpoint, whole repo), lenses security-auth, real-data, plus dependency vetting (go.mod is new). Not covered: application logic (audit tier), testing and production-readiness lenses, anything needing the live deploy (Traefik forward-auth binding, X-Real-Ip handling). Last audit: never. Runbooks executed by an Opus reviewer; gitleaks over 17 commits and the git history grep found nothing; SQL uses placeholders only; modernc.org/sqlite v1.58.0 is current, BSD-3, zero advisories, go.sum committed.
### Findings
- [x] IMPORTANT | security-auth | admin authorization was a self asserted header, so anything reaching the container socket was admin (internal/server/server.go:107) | Closed 2026-09-11: ITWORKS_ADMIN_USERS allowlist, empty means deny all; TestAdminGuardUserNotInAllowlist403 and TestAdminGuardEmptyAllowlistDeniesAll403 pass; live GET /admin with header anyone returned 403
- [x] IMPORTANT | security-auth | any non empty username passed, so every Authentik account was an itworks admin (internal/server/server.go:110) | Closed 2026-09-11: same allowlist; compose sets ITWORKS_ADMIN_USERS=matthew, to be confirmed against the forwarded X-authentik-username at first deploy
- [x] ADVISORY | security-auth | the per IP limiter ran after validation, so malformed POSTs were never counted (internal/server/handlers.go:102) | Closed 2026-09-11: limiter now runs first; TestRateLimitCountsMalformedPosts429 passes (five 415s then 429)
- [ ] ADVISORY | security-auth | with ITWORKS_TRUST_PROXY=1 the limiter keys on X-Real-Ip, which is only safe if Traefik overwrites it (internal/server/server.go:135) | Evidence to close: not verified live: for i in $(seq 1 6); do curl -s -o /dev/null -w "%{http_code}\n" -H "X-Real-Ip: 10.0.0.$i" -H "Content-Type: application/json" -d @fixtures/seed-msp-sentinel.json https://itworks.dev/api/entries; done shows 429 on the sixth
- [x] ADVISORY | real-data | the masthead rendered a fabricated green badge with no example caption (web/templates/landing.html:7) | Closed 2026-09-11: masthead shows the newest approved entry's real badge, or the green sample captioned example when the wall is empty
